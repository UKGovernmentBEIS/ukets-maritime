import { EmissionsMonitoringPlanNotification } from '@mrtm/api';

import { SubTaskListMap } from '@shared/types';

export const detailsChangeMap: SubTaskListMap<EmissionsMonitoringPlanNotification> = {
  title: 'Details of the change',
  detailsOfChange: {
    title: 'Non-significant change to the emissions monitoring plan',
  },
};
