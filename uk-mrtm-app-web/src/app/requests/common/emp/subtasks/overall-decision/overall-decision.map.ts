import { SubTaskListMap } from '@shared/types';

export const overallDecisionMap: SubTaskListMap<{
  actions: string;
}> = {
  title: 'Overall decision',
  actions: {
    title: 'Available actions',
  },
};
