server {
  listen 80;
  charset UTF-8;

  location /maritime/ {
    root /usr/share/nginx/html;
    index index.html index.htm;
    try_files $uri $uri/ /maritime/index.html =404;
  }

  location / {
    return 301 /maritime/;
  }

  include /etc/nginx/extra-conf.d/*.conf;
}
